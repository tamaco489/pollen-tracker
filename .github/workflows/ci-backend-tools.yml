name: Backend Tools CI

on:
  push:
    branches: [main]
    paths:
      - "backend/tools/**"
      - ".github/workflows/ci-backend-tools.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/tools/**"
      - ".github/workflows/ci-backend-tools.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Node.js 24 がデフォルト化される 2026-06-02 以降は削除可

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: "backend/go.mod"
          cache-dependency-path: "backend/go.sum"

      - name: Cache golangci-lint binary
        id: cache-golangci-lint
        uses: actions/cache@v4
        with:
          path: ~/go/bin/golangci-lint
          key: ${{ runner.os }}-golangci-lint-${{ hashFiles('backend/go.sum') }}

      - name: Install golangci-lint
        if: steps.cache-golangci-lint.outputs.cache-hit != 'true'
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

      - name: Lint
        run: make lint-tools

  sqlc-lint:
    name: sqlc Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: "backend/go.mod"
          cache-dependency-path: "backend/go.sum"

      - name: Cache sqlc binary
        id: cache-sqlc
        uses: actions/cache@v4
        with:
          path: ~/go/bin/sqlc
          key: ${{ runner.os }}-sqlc-${{ hashFiles('backend/go.sum') }}

      - name: Install sqlc
        if: steps.cache-sqlc.outputs.cache-hit != 'true'
        run: go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest

      - name: sqlc Lint
        run: make sqlc-lint
